# 🛍 Product Search Validator (Test Task)

Это проект — мост (Proxy-валидатор) между клиентской частью и API магазина. Он не просто ищет товары, а гарантирует, что каждый запрос и ответ соответствуют строгому «контракту» данных.

---

## 🛠 Как это устроено?

### 1. Frontend (Nuxt 3 + Tailwind)
* **Интерфейс поиска**: Лаконичный и отзывчивый UI.
* **Умное поле ввода**: Распознает обычный текст и сырые JSON-запросы.
* **Контроль валидации**: Визуально отображает ошибки сервера.

### 2. Backend (Fastify + Zod)
* **Защитный слой**: Проверяет входящий запрос через схемы Zod.
* **Proxy-логика**: Пересылает валидные запросы на удаленный сервер.
* **Swagger UI**: Интерактивная документация с описанием всех запросов и ответов.

---

## 📖 Документация API (Swagger)

Это ключевая часть задания. Здесь реализовано:
* **Request Body**: Полное описание объектов `filters` и `paginate`.
* **Responses**: Структура успешного ответа и детальные ошибки валидации.
* **Schemas**: Модели данных (`Product`, `SearchResponse`) вынесены в отдельные схемы.

**Доступ к Swagger**: [http://localhost:3000/documentation](http://localhost:3000/documentation)

---

## 🚀 Быстрый запуск

### 1. Запуск Backend (Порт 3000)

```bash
cd backend
npm install
npm run dev
```

* **API адрес**: http://localhost:3000

* **Swagger**: http://localhost:3000/documentation

### 2. Запуск Frontend (Порт 3001)
```Bash

cd frontend
npm install
npm run dev
```

* **Интерфейс**: http://localhost:3001

    Важно: Если порт 3000 занят бэкендом, Nuxt предложит использовать порт 3001. Нажмите Y.

## 🧪 Как проверять?
### Сценарий А: Обычный поиск

Введите слово кольцо в поле поиска. Фронтенд упакует его в объект `filters: { search_string: "кольцо" }`.
### Сценарий Б: Проверка JSON-запроса

Вставьте в поле поиска сырой JSON:
```JSON
{
  "filters": { "search_string": "тест" },
  "paginate": { "page": 1, "pp_items": 5 }
}
```

### Сценарий В: Валидация ошибок

Отправьте неверный JSON, например:
JSON

```JSON
{ "paginate": { "page": "первая" } }
```

Вы увидите сообщение об ошибке валидации от Zod.
## 📂 Структура данных

Контракт данных описан в `frontend/types/api.ts` и дублируется схемами Zod на бэкенде.